{
  "name": "todo-crud-challenge",
  "version": "0.1.0",
  "repository": "git@github.com:elton-okawa/todo-crud-challenge.git",
  "author": "Elton Okawa <elton.okawa@gmail.com>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start:dev": "concurrently --names 'SERVER,REACT,RELAY' -c 'green,blue,yellow' 'cd api && yarn start:dev' 'cd app && yarn watch:react' 'cd app && yarn watch:relay'",
    "postinstall": "concurrently --names 'BACKEND,FRONTEND' 'cd api && yarn' 'cd app && yarn'"
  },
  "devDependencies": {
    "concurrently": "^8.0.1"
  }
}
